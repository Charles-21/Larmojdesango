Para clonar un repositorio:

git clone (link https del repositorio)

Para subir cambios a un repositorio utiliza la siguiente lista de comandos:

git status
git add .
git commit -m "comentario sobre los cambios"
git push origin main
